$kind: http-example
request:
  url: "{{baseUrl}}/ai/recommend"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
  body:
    type: json
    content: |-
      {
        "budget": 5887.233058458011,
        "check_in": "string",
        "check_out": "string",
        "preferences": "string",
        "query": "string"
      }
response:
  statusCode: 422
  statusText: Unprocessable Entity (WebDAV) (RFC 4918)
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "key_0": "string",
        "key_1": "string"
      }
order: 3000
